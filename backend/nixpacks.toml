[providers]
python = "3.11"

[phases.setup]
nixPkgs = ["python311", "pip311"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "cd backend || true",
  "pip install -r requirements-core.txt"
]

[start]
cmd = "cd backend && python app.py"

